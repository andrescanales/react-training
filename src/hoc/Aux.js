// Higher Order Component
// This component might look useless beacuse all it does is returning
// what's passed between the opening and closing tags. So it's only
// wraping another component content.
const aux = (props) => props.children;

export default aux;